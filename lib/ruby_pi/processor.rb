class Processor

  def initialize()
    # @mapping = mapping
  end

  def process(state)
    # state.states.each do |state|
    #   @mapping[state]
    # end
    ## Output to GPIO
    # puts "#{Time.now} - #{state.states}"
  end

end
